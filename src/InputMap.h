#pragma once

enum KEY_ARROWS {UP, DOWN, LEFT, RIGHT};
enum KEY_GENERAL {SPACE, CTRL};
extern bool key_arrows[4];
extern bool key_general[2];
	
enum MOUSEBUTTONS {LMB, RMB};
extern bool mouse[2];

extern float mouseX;
extern float mouseY;